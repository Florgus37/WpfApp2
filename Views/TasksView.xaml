<UserControl x:Class="WpfApp2.Views.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="150" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="200" Margin="10,0" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Width="80" SelectedItem="{Binding Priority}">
                <ComboBoxItem Content="Низкий"/>
                <ComboBoxItem Content="Средний"/>
                <ComboBoxItem Content="Высокий"/>
            </ComboBox>
            <DatePicker Width="100" SelectedDate="{Binding DueDate}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredTasks}" SelectedItem="{Binding SelectedTask}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Готово" Binding="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Приоритет" Binding="{Binding Priority}"/>
                <DataGridTextColumn Header="Срок" Binding="{Binding DueDate, StringFormat=\{0:dd.MM.yyyy\}}"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Priority}" Value="Высокий">
                            <Setter Property="Background" Value="LightCoral"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                            <Setter Property="Background" Value="LightGray"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <ComboBox SelectedItem="{Binding Filter}" Margin="5">
                <ComboBoxItem Content="Все"/>
                <ComboBoxItem Content="Active"/>
                <ComboBoxItem Content="Completed"/>
            </ComboBox>
            <Button Content="Добавить" Command="{Binding AddTaskCommand}" Margin="10,0,5,0"/>
            <Button Content="Удалить" Command="{Binding DeleteTaskCommand}" Margin="0,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>