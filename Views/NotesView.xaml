<UserControl x:Class="WpfApp2.Views.NotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />

            <ListBox ItemsSource="{Binding DisplayedNotes}" DisplayMemberPath="Title" SelectedItem="{Binding SelectedNote}"/>
        </StackPanel>

<StackPanel Grid.Column="1">
    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
    <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="300" Margin="0,0,0,10"/>
    <StackPanel Orientation="Horizontal">
        <Button Content="Сохранить" Command="{Binding SaveNoteCommand}" Margin="0,0,5,0"/>
        <Button Content="Новая заметка" Command="{Binding NewNoteCommand}" Margin="0,0,5,0"/>
        <Button Content="Удалить" Command="{Binding DeleteNoteCommand}"/>
    </StackPanel>
</StackPanel>
    </Grid>
</UserControl>